# WonderLife 작업 지침

- 프로젝트명은 **WonderLife**이며 일상에서 반복해서 쓰는 정확한 계산 도구를 제공한다.
- PixelLife와 동일하게 Java 17 + Spring Boot 3.5, Maven, React + TypeScript + Vite, MyBatis + Flyway, Aiven MySQL, Google OAuth 2.0 서버 세션을 사용한다.
- 영어를 기본 언어로 하되 한국어, 중국어 간체, 일본어를 지원할 수 있는 문자열 구조를 유지한다.
- 비회원은 모든 계산기를 사용할 수 있지만 계산 입력·결과를 브라우저나 서버에 저장하지 않는다.
- Google 로그인 회원만 계산 이력을 서버 DB에 저장하고 본인 이력만 조회·삭제할 수 있다.
- 금액은 정수 원, 이율과 비율은 내부 계산에서 충분한 정밀도를 유지한 뒤 표시 단계에서만 반올림한다.
- 날짜는 `YYYY-MM-DD` 달력 날짜로 계산하고 시간대 계산은 IANA 시간대를 사용한다.
- 금융·세금·보험 결과는 선택 연도의 공시 기준과 가정을 함께 표시하고 실제 계약·급여명세와 다를 수 있음을 알린다.
- 적용된 Flyway 마이그레이션은 수정하지 않고 새 버전을 추가한다.
- 비밀값을 소스에 저장하지 않는다. `.env.example`에는 placeholder만 둔다.
- 모바일 우선, 키보드 조작, 명확한 대비, 모션 감소 설정을 지원한다.
- 커밋과 푸시는 사용자가 직접 한다. 에이전트는 실행하지 않는다.

